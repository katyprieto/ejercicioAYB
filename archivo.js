function puedeSubir(altura, acompanada){
    if (altura >= 1.40 && altura <= 2) 
        return true;
    }
    if( altura <= 1.40 && altura >= 1.20 && acompanada){
        return true;
    }
    else{
        return false;
    }